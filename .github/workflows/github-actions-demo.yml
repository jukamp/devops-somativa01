name: CI-Somativa

on:
    push:
        branches: [ 'master' ]
    pull_request:
        branches: [ 'master' ]

    workflow_dispatch:

jobs:
    build-job:
        runs-on: ubunto-latest
        steps:
            - uses: actions/checkout@v3
            - run: python3 -v

    teste-job:
        runs-on: ubunto-latest
        steps:
            - uses: actions/chechout@v3
            - run: pyhton3 helloworld.py

    deploy-job:
        runs-on: ubunto-latest
        steps:
            - uses: actions/chechout@v3
            - run: echo 'Finalizado!'